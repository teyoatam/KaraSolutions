# models/doctors_et_transformation.yml
version: 2
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  - name: doctors_et_transformation
    tests:
      - not_null:
          column_name: message_id
      - not_null:
          column_name: date
